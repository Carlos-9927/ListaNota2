#include <stdio.h>

int main()
{
    int n;
    int ant;
    int suc;
    
    printf("Digite o número :");
    scanf("%d", &n);
    ant = (n - 1);
    sus = (n + 1);
    printf("seu antecessor é %d", ant);
    printf("\nseu sucessor é %d", sus);

    return 0;
}